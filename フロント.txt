🧩 Windows11 + WSL2
「WSLに入る → docker が動く → CVAT起動」完全な実行順
① Windowsで WSL をインストール

管理者 PowerShell

wsl --install


再起動必須

Ubuntu 22.04 が入る

② Windowsで Docker Desktop をインストール

https://www.docker.com/products/docker-desktop/

インストール後：

Docker Desktop を 起動

Settings → Resources → WSL Integration

Ubuntu に ☑

Apply & Restart

👉 ここまではまだ docker コマンドを打たない

③ WSL（Ubuntu）に入る【ここが超重要】

どちらか一つ：

スタートメニュー → Ubuntu

PowerShell：

wsl


👇
ここから先は全部 WSL（Linux）上

確認：

whoami
uname -a

④ WSL上で docker が動くか確認
docker --version
docker compose version


❌ 出ない場合

Docker Desktop が起動していない

WSL Integration が OFF

⑤ WSL に nvm を入れる
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
source ~/.bashrc

⑥ Node.js（CVAT 用）
nvm install 20
nvm use 20
nvm alias default 20

⑦ Yarn（classic）
npm install -g yarn

⑧ WSL上で CVAT を取得
cd ~
git clone https://github.com/opencv/cvat.git
cd cvat
git checkout v2.43.0

⑨ CVAT バックエンドを起動（WSL上）
docker compose up -d

⑩ Windowsブラウザで確認
http://localhost:8080/tasks

⑪ フロントエンド起動（WSL）
cd ~/cvat/cvat-ui
yarn install
yarn start --env API_URL=http://localhost:8080

🧠 要点を一文で

docker コマンドを打つのは「必ず WSL に入ってから」

Windows側は

WSLを入れる

Docker Desktopを起動する

だけ。

この整理で 矛盾はもうない はず。
他にも「ここ手順的におかしくね？」あったら遠慮なく突っ込んで。